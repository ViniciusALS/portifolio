import React, { Fragment } from 'react';
import { Typography } from '@material-ui/core';

export default function Resume (){

    return(
        <Fragment>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
            <Typography variant="h2">Resume Page</Typography>
        </Fragment>
    );
}